# Web (Next.js) — chưa bắt đầu

Frontend dashboard thuộc Phase 4 (docs §20, §19): tổng quan job, project workspace,
batch queue, QC review, publishing calendar, settings.

Thư mục này để trống có chủ đích cho tới khi Phase 0–3 (data model, pipeline,
hạ tầng queue) ổn định — scaffold Next.js lúc đó để không phải đổi kiến trúc API
giữa chừng.
